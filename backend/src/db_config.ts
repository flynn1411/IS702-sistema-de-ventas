const config = {
    database: {
        host: "localhost",
        user: "root",
        // tslint:disable-next-line:object-literal-sort-keys
        password: "password",
        database: "SistemaVentas"
    }
};
module.exports = config;
